# Tasks

- [ ] Core functions for each overlay field (humidity, sunlight, temperature,
      rain rate).
- [ ] The overlay cube, filled only while an overlay shows.
- [ ] The overlay pass: colour ramps and streamlines; naga-validated; in the
      body-local frame.
- [ ] `OverlayMode`, the M key, a pause-menu row per overlay.
- [ ] The legend, with the ramp image held to the shader's table.
- [ ] Captures from orbit, one per overlay; streak alignment measured.
- [ ] fmt, clippy, tests, `openspec validate --all`.
