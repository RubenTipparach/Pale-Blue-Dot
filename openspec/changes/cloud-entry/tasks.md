# Tasks

- [x] 1. The resolve writes the history's distances too (the clip itself is `cloud-history-clip`); the march writes this frame's cloud, distances and near field
- [x] 2. Less raw noise: interleaved gradient jitter, the footprint floor at half a step, the third shape octave faded, the sea's hit over four corners
- [x] 3. The near field: the `near` target, two coarse samples lit once, the composite's slab
- [x] 4. The GPU tests; the limb frame (the faint stepped line batch 1 left is gone); the turn check (no print of the tool); cloud-hop and scenic recordings (the entry fades into fog with no salt, bands or curtain, `output/rec3`); the perf suite
