# Tasks

- [ ] 1. The resolve pass: `current`/`current_depth` targets, `cloud_resolve` with the neighbourhood clip over `cloud-ghosting`'s taps and `cloud-edges`' distances, the composite reading its output
- [ ] 2. Less raw noise: interleaved gradient jitter, the footprint floor at half a step, the third shape octave faded, the sea's hit over four corners
- [ ] 3. The near field: the `near` target, two coarse samples lit once, the composite's slab
- [ ] 4. The GPU tests; the limb frame; the turn check; a cloud-hop and a scenic recording; the perf suite
