# Tasks

- [x] 1. The march's tail: `CLOUD_TAIL_STEPS`; past the budget each step is at least what is left of the span, without the gap under the base, over the iterations left
- [x] 2. `CLOUD_TAIL_STEPS` chosen from stills at the owner's frame against the 256-step reference (16: 8 leaves the jitter's crosshatch on the far deck); the arc's brightening 13 -> 2 grey levels (256 steps: 1.5)
- [x] 3. Stills in and around the layer (200, 400, 600 and 1000 m); the `--turn` check (still on its floor, turning 0.000%)
- [x] 4. The perf suite, old against new, on `clouds`, `cloud-hop` and `storm` (`docs/benchmarks/2026-09-26-cloud-reach`): +0.3 ms of GPU in cloud on `clouds`, +0.15 on `cloud-hop`, the storm unchanged; p95 and p99 within the spread
