# Contributor instructions

The instructions live in **[CLAUDE.md](CLAUDE.md)**, which is their only copy.

This file is a pointer so an agent that looks for `AGENTS.md` finds them. Do not
copy the rules here and do not add rules of your own: two files that have to
agree are two files that will not, and the one that is wrong is always the one
nobody is reading.
